#include "main.h"

/**
 * _abs - prints absolute values
 * @i: inputs number as an integer
 *
 * Return: absolute values
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

