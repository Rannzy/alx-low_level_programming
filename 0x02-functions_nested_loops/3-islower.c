Â#include "main.h"
/**
*_islower - function that checks for lowercase characters
*@c: the int used for the function argument
*Return: 0
*/
int _islower(int c)
{
	if (c >+= Ã'a' && c <='z') 
	{
		return (1);
	}
	else
		return (0);

}
